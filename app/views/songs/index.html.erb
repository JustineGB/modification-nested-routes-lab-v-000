<h1>Song Index</h1>
  <ul>
    <% @songs.each do |song| %>
      <li><%= display_artist(song) %> - <%= link_to song.title, song_path(song) %></li>
    <% end %>
  </ul><br>
<%= link_to "Add New Song", new_song_path %>


<!--<%# @songs.each do |song| %>
    <h2><%#= song.title %></h2>
    <h3>by: <%#= link_to song.artist.name, artist_songs_path(song.artist) %></h3>
<%# end %>-->

